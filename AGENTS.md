# sub2api 仓库级执行约束

## 1. 语言与沟通

- 默认使用中文说明分析、计划、变更与验证结果。
- 先给结论，再给关键细节。

## 2. 代码修改基本原则

- 先定位影响范围，再动手修改。
- 修改后不得保留失效实现、失效引用、无用测试或半删除状态。
- 优先补齐或更新测试，保证改动可验证。

## 3. 发布要求

- 发布前先确认工作区状态符合预期。
- 如果已经推送了错误 tag，需要修复后再处理 tag；除非用户明确要求，否则不要改写已发布历史。
- 如果用户明确要求 force 更新已发布 tag，仍然必须先完成完整验证，再执行 force。

## 4. 终端执行要求

- 路径统一使用双引号包裹。
- 执行发布相关命令时，输出中必须包含已执行的验证步骤与结果摘要。

## 5. fork 的作用与使用边界

- `fork` 的核心作用是：在同步 `upstream/main` 时提供一个受控缓冲层，避免冲突处理把本地不希望变动的实现改坏或改丢。
- 合并冲突时，先按功能域分层处理：对用户明确要求保持本地实现的区域（例如 image 生图链路）优先保留本地版本，再吸收上游必要修复。
- 任何会改变既定本地产品行为的冲突解法，必须先记录到 `fork.md`，说明原因、取舍和回退方式，再执行后续提交。
- `fork.md` 是 fork 决策与用户偏好的落地台账；涉及合并策略、冲突偏好、保留/舍弃规则时，必须同步更新。
