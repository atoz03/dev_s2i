# sub2api 仓库级执行约束

## 1. 语言与沟通

- 默认使用中文说明分析、计划、变更与验证结果。
- 先给结论，再给关键细节。

## 2. 代码修改基本原则

- 先定位影响范围，再动手修改。
- 修改后不得保留失效实现、失效引用、无用测试或半删除状态。
- 优先补齐或更新测试，保证改动可验证。

## 3. Push / Tag 前强制检查

**任何准备执行 `git push`、发布 tag、或更新已发布 tag 之前，必须先在本地完成 test 和 lint，并确认通过。禁止跳过。**

### 后端最少检查

```bash
cd backend
go test ./...
golangci-lint run --timeout=30m
```

如改动涉及仅 unit 测试或 CI 与本地命令存在差异，也需要至少保证最终执行过与 CI 等价或更严格的检查。

### 前端最少检查

```bash
cd frontend
pnpm test:run
pnpm lint:check
pnpm typecheck
```

如果只跑了定向测试，必须明确说明原因；在发布前应尽量补跑完整检查。

## 4. 发布要求

- 发布前先确认工作区干净、测试通过、lint 通过。
- 如果已经推送了错误 tag，需要修复后再处理 tag；除非用户明确要求，否则不要改写已发布历史。
- 如果用户明确要求 force 更新已发布 tag，仍然必须先完成完整验证，再执行 force。

## 5. 终端执行要求

- 路径统一使用双引号包裹。
- 执行发布相关命令时，输出中必须包含已执行的验证步骤与结果摘要。
